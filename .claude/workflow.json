{
  "$comment": "Per-repo values for the workflow plugin and skills. Skills hold the rules. docs/ holds the reasoning.",
  "packageManager": "pnpm",
  "leanGate": {
    "command": "pnpm test:all",
    "contents": "php syntax lint + WordPress 6.7/PHP 7.4 suite + render checks"
  },
  "contractStep": {
    "description": "tests/atlas-url-contract.json must match SahajCloud's copy byte for byte. CI fails on drift.",
    "command": "curl -fsSL --retry 3 https://raw.githubusercontent.com/sydevs/SahajCloud/main/src/lib/atlas/atlas-url-contract.json -o /tmp/upstream.json && diff -u /tmp/upstream.json tests/atlas-url-contract.json"
  },
  "securityReview": {
    "triggerPattern": "^includes/|^sahaj-atlas\\.php|^blocks/|^templates/|^\\.distignore|^\\.github/workflows/release\\.yml",
    "rationale": "The plugin runs on 13 volunteer sites. .distignore sets what ships. See AGENTS.md."
  },
  "worktreeSetup": [
    "pnpm install"
  ],
  "deployTarget": "GitHub Releases zip (built by .github/workflows/release.yml)",
  "devServer": {
    "command": "pnpm start",
    "basePort": 8881,
    "healthPath": "/",
    "isolateDatabase": false
  },
  "generatedFiles": [
    {
      "pattern": "^vendor/",
      "reason": "Third-party code, vendored into the plugin. Do not edit it. Update the upstream dependency instead."
    },
    {
      "pattern": "^tests/atlas-url-contract\\.json$",
      "reason": "Must stay byte-identical to SahajCloud's copy. CI diffs it and fails on drift. Update it only as the downstream half of a /cross-repo-issue, after SahajCloud merges to main."
    },
    {
      "pattern": "^pnpm-lock\\.yaml$",
      "reason": "Generated by pnpm. Use `pnpm add`, `pnpm remove`, or `pnpm update` instead."
    }
  ],
  "prAllowlistGlobs": [
    "package.json",
    "pnpm-lock.yaml",
    "**/*.md",
    ".claude/**",
    "tests/atlas-url-contract.json"
  ]
}
