{
  "$comment": "Per-repo values read by the sydevs `workflow` plugin and its skills. Values only \u2014 the rules that consume them live in the skills, the stories in docs/.",
  "packageManager": "pnpm",
  "leanGate": {
    "command": "pnpm test:all",
    "contents": "php syntax lint + WordPress 6.7/PHP 7.4 suite + render checks"
  },
  "contractStep": {
    "description": "tests/atlas-url-contract.json must stay byte-identical to SahajCloud's copy; CI fails loudly on drift.",
    "command": "curl -fsSL --retry 3 https://raw.githubusercontent.com/sydevs/SahajCloud/main/src/lib/atlas/atlas-url-contract.json -o /tmp/upstream.json && diff -u /tmp/upstream.json tests/atlas-url-contract.json"
  },
  "securityReview": {
    "triggerPattern": "^includes/|^sahaj-atlas\\.php|^blocks/|^templates/|^\\.distignore|^\\.github/workflows/release\\.yml",
    "rationale": "AGENTS.md \u2014 the plugin renders on 13 volunteer-run sites; .distignore governs what ships."
  },
  "worktreeSetup": [
    "pnpm install"
  ],
  "deployTarget": "GitHub Releases zip (built by .github/workflows/release.yml)",
  "devServer": {
    "command": "pnpm start",
    "basePort": 8881,
    "healthPath": "/",
    "isolateDatabase": false
  },
  "generatedFiles": [
    {
      "pattern": "^vendor/",
      "reason": "`vendor/` is third-party code vendored into the plugin. Do not edit it \u2014 update the upstream dependency instead."
    },
    {
      "pattern": "^tests/atlas-url-contract\\.json$",
      "reason": "This file must stay byte-identical to SahajCloud's copy; CI diffs it and fails on drift. Update it only as the downstream half of a /cross-repo-issue, after the SahajCloud change is merged to main."
    },
    {
      "pattern": "^pnpm-lock\\.yaml$",
      "reason": "`pnpm-lock.yaml` is generated by pnpm. Use `pnpm add` / `pnpm remove` / `pnpm update`."
    }
  ],
  "prAllowlistGlobs": [
    "package.json",
    "pnpm-lock.yaml",
    "**/*.md",
    ".claude/**",
    "tests/atlas-url-contract.json"
  ]
}
